[package]
name = "rust-deflate"
version = "0.3.0"
edition = "2024"
rust-version = "1.85"
description = "A DEFLATE (RFC 1951) and zlib (RFC 1950) decompressor written from scratch in Rust."
license = "MIT"
readme = "README.md"
repository = "https://github.com/YohanSandun/deflate"
keywords = ["deflate", "zlib", "inflate", "decompression", "compression"]
categories = ["compression"]

[dependencies]

[profile.test]
opt-level = 1
